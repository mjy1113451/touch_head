内容预览：
# astrbot_plugin_petpet 依赖包
# 摸头杀（petpet）GIF 插件所需依赖

# 图像处理库 - 用于生成和处理GIF图片
Pillow>=10.0.0

# 异步HTTP客户端 - 用于调用外部API获取资源
httpx>=0.25.0

# 图像处理增强库 - 用于高级图像操作
numpy>=1.24.0

# 可选：图像滤镜和特效
# opencv-python>=4.8.0  # 如果需要更复杂的图像处理

# 可选：缓存支持 - 用于缓存生成的GIF
# diskcache>=5.6.0

# 可选：进度条显示 - 用于调试
# tqdm>=4.66.0
